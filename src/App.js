
// Импортирует компонент Layout из папки components/Layout
import Layout from './components/Layout/Layout';

// Объявляет функциональный компонент App
function App() {
  // Возвращает JSX-разметку, которая будет отрендерена
  return (
    // Рендерит компонент Layout, который оборачивает дочерние элементы
    <Layout>
     
    </Layout>
  );
}

// Экспортирует компонент App для использования в других файлах
export default App;
